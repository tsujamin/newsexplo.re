[Unit]
Description=gunicorn daemon
After=network.target

[Service]
PIDFile=/tmp/gunicorn.pid
User=govhack
Group=govhack
WorkingDirectory=/srv/govhack2016/backend
ExecStart=/usr/bin/bash -c "cd /srv/govhack2016; source bin/activate; gunicorn --timeout 60 --workers 4 --pid /tmp/gunicorn.pid backend:app --log-level debug --log-file -"
ExecReload=/bin/kill -s HUP $MAINPID
ExecStop=/bin/kill -s TERM $MAINPID
PrivateTmp=true

[Install]
WantedBy=multi-user.target
